# سيارات مجمع الثمامة (الهلال الأحمر القطري): نظام إدارة النقل الطبي

- **الموقع:** https://althumamacar.sifr-qr.com على استضافة Hostinger (حساب `sifrdigital.qr@gmail.com`، والمستخدم متعاون فيه). لا يُستخدم Firebase إطلاقًا.
- **النشر من جهاز المستخدم (Windows):** `git checkout main` ← `git pull` ← `pnpm install` ← `pnpm run deploy`، فيُنشأ `dist/althumama-hostinger.zip`، ثم يُرفع إلى `public_html` من File Manager ويُختار Extract. يجب كتابة `run` لأن `pnpm deploy` وحده أمر مدمج في pnpm. بيئة Claude السحابية لا تستطيع النشر ولا الوصول إلى الموقع.
- **الإعداد الأول (مرة واحدة):** إنشاء قاعدة MySQL في hPanel، ثم فتح `/api/setup.php` وإدخال بياناتها وكلمة مرور المدير `admin`. يُحفظ الإعداد في `althumama-config.php` خارج `public_html`، وتتوقف صفحة الإعداد بعدها. يلزم PHP 8.1 أو أحدث.
- المستخدم يتحدث العربية؛ رُدّ بالعربية.

## التقنية
- React وVite وTailwind وTypeScript (الواجهة في `client/`، والمنطق المشترك في `shared/`).
- **الخادم `api/` (PHP مع MySQL):** `index.php?r=<route>` لتسجيل الدخول والمستخدمين والمزامنة والكتابة وموقع السائق والإحصائيات، و`setup.php` للإعداد الأول. الجلسة كوكي PHP (`HttpOnly` و`SameSite=Strict`) تنتهي بإغلاق المتصفح أو بعد ساعة بلا نشاط. كلمات المرور بـ `password_hash`، والدخول يتوقف 15 دقيقة بعد 10 محاولات خاطئة للحساب. الطلبات التي تغيّر البيانات تحتاج ترويسة `X-Requested-With: fetch`.
- **الجداول:** `users`، و`docs` (مجموعة `col` ومعرّف `id` ومحتوى JSON، و`rev` للمزامنة، وقيمة `data = NULL` تعني أن العنصر محذوف)، و`revision`، و`login_attempts`.
- **الصلاحيات حسب الدور في `api/lib/rules.php`:** مشرف المبنى يغيّر حالة الموعد والطلب فقط، ومشرف السيارات يغيّر خانات الإرسال في الطلب فقط، والسائق يرسل موقع سيارته فقط عبر `location`.
- الخرائط بمكتبة Leaflet مع OpenStreetMap، والمخططات بمكتبة recharts.
- مجلد `server/` فيه اختبارات vitest فقط (أُزيل خادم قالب Manus القديم).

## الأدوار
- **المدير (admin):** المستخدمون والسيارات ودليل المستشفيات والخريطة والإحصائيات. الحساب الأول `Admin` / `Admin123` يُنشأ عند أول دخول ويُجبر على تغيير كلمة المرور.
- **العيادة (clinic):** المواعيد الطبية بالعربية والإنجليزية (`client/src/lib/i18n.ts`)، مع اختيار التاريخ اليوم / غدًا / تاريخ آخر.
- **مشرف المبنى (buildingSupervisor):** مواعيد اليوم فقط؛ يطلب السيارة، ويؤكد وصولها واستلام المريض، ويطلب العودة.
- **مشرف السيارات (fleetSupervisor):** التوزيع، وجمع الرحلات، ورسالة واتساب للسائق، والرحلات غير الطبية، واختيار التاريخ.
- **السائق (driver):** مرتبط بسيارة (`vehiclePlate`)، ويرسل موقعه من المتصفح إلى `vehicleLocations/{plate}`.

## قواعد العمل (`shared/transport.ts`)
- لا يُطلب سيارة بعد مرور 30 دقيقة على الموعد (`requestWindow`)، إلا إذا عدّلت العيادة الموعد.
- **الجمع:** حتى 3 مرضى في السيارة، واثنان لسيارة الاحتياجات الخاصة (`buildTripGroups`). المستشفيات حتى 3 كم تُعتبر متجاورة، وحتى 8 كم في نفس الاتجاه.
- **ضم مريض إلى سيارة في الطريق:** `suggestJoinDispatched`.
- **تنبيه:** مواعيد لنفس الوجهة وخلال 30 دقيقة لم يُطلب لها سيارة (`findUnrequestedMatches`).
- **الرحلات غير الطبية:** `category: "غير طبية"`، والوجهات: الجامعة، المدرسة، أنصار جاليري المطار القديم، أو «أخرى» مكتوبة.
- **رسالة السائق:** بالعربية ثم الإنجليزية (`buildDriverMessage`)، عبر رابط `wa.me` برمز قطر 974.

## البيانات
- `shared/seedData.ts`: 22 سيارة، و`shared/historySeed.ts`: ملخص إحصائيات الفترة 11-07 إلى 23-08-2026 بلا بيانات مرضى (في `meta/history`). يُحفظان عند دخول المدير إن لم يكونا موجودين.
- **الإحصائيات (`shared/stats.ts` و`StatsPanel.tsx`):** سجلات رحلات بلا بيانات مرضى. ملفات Excel تُحفظ يومًا بيوم في `statsDays/{date}` (الرفع الجديد يضيف أيامه أو يستبدلها فقط)، ورحلات النظام تُحسب مباشرة من المواعيد والطلبات. لكل يوم مصدر واحد: ملف Excel، ثم الملخص القديم، ثم النظام. الملخص القديم بلا تفاصيل يومية للساعات والوجهات والسيارات، فتفاصيله تظهر فقط عند عرض كامل فترته بلا فلاتر.
- `shared/hospitals.ts`: 23 مستشفى؛ الخانة `verified` تعني إحداثيات مؤكدة. `syncHospitals` يحدّث المستشفيات غير المؤكدة المحفوظة.
- `client/src/lib/appStore.ts` و`apiBackend.ts`: كل قائمة مجموعة في قاعدة البيانات. لا يُكتب إلا ما تغيّر، بالمقارنة مع القائمة التي عدّلها المستخدم (`saveState(key, next, baseline)`)، والعنصر الموجود تُكتب خاناته المتغيرة فقط (`op: "update"`). كل حفظ دفعة واحدة (كلها أو لا شيء). تحديث الشاشة من المستخدمين الآخرين بسؤال `sync?since=<rev>` كل 4 ثوانٍ.
- `client/src/lib/auth.ts` و`api.ts`: الجلسة والمستخدمون عبر الخادم. أي رد 401 يُخرج المستخدم، والحساب يُفحص كل 30 ثانية.
- الخريطة والإحصائيات ولوحة المدير وصفحة السائق تُحمَّل عند الحاجة (`React.lazy`).
- **ممنوع رفع بيانات المرضى** (الأسماء والأرقام والشقق) إلى المستودع.

## التحقق قبل الدفع
- `pnpm check` و`pnpm test` و`npx vite build`.
- `php -l api/index.php api/setup.php api/lib/*.php`، و`pnpm run deploy` (يبني الموقع وملف الرفع).
- التجربة المحلية: MySQL أو MariaDB محلي، ثم `pnpm run dev:api` (خادم PHP على المنفذ 8000)، و`pnpm dev` (يمرّر `/api` إليه)، والإعداد من `http://localhost:5173/api/setup.php`.

## أمور معلّقة
- إعادة رفع ملف Excel للفترة 11-07 إلى 23-08-2026 من لوحة المدير لتفعيل الفلترة الكاملة لتلك الفترة.
- شعار الهلال الأحمر الرسمي: يوضع في `client/public/qrcs-logo.png`، وحاليًا يظهر هلال مرسوم بالكود.
- التأكد بعد النشر من أن مشاركة الموقع تعمل في صفحة السائق، وأن السيارات المضافة تظهر لمشرف السيارات.
- التتبع الدائم بأجهزة GPS يحتاج جهاز تتبع في السيارة يرسل إلى `api/index.php?r=location` أو نقطة مخصصة له.
