# متابعة تطوير سيارات مجمع الثمامة

## دورة طلب وتوزيع السيارة

- [x] تحويل طلب مشرف المبنى إلى تنبيه يظهر لدى مشرف السيارات دون إظهار سيارة أو سائق مسبقًا.
- [x] إضافة صندوق تنبيهات للطلبات الجديدة في صفحة مشرف السيارات.
- [x] إضافة اختيار السيارة والسائق وإجراء «إرسال السيارة» من صفحة مشرف السيارات.
- [x] إظهار بيانات السيارة والسائق لمشرف المبنى بعد الإرسال فقط.
- [x] منع اقتراح السيارات غير المتاحة أو المرتبطة برحلات نشطة.
- [x] إضافة إلغاء طلب السيارة من صفحة مشرف المبنى قبل وصول السيارة.
- [x] إضافة فلتر سريع للمواعيد حسب رقم المبنى.
- [x] إضافة اقتراحات جمع الرحلات حسب الوقت والمبنى والوجهة ونوع الرحلة.
- [x] إضافة معادلة نقاط واضحة وقرار جمع عند 55 نقطة فأكثر وخلال 45 دقيقة.
- [x] دعم إرسال سيارة واحدة لمجموعة رحلات مقترحة وتسجيل رقم المجموعة.
- [x] ترحيل الطلبات المحفوظة سابقًا إلى حالات دورة التوزيع الجديدة.

## ملاحظة التسليم

بناءً على طلب المستخدم، لم تُشغّل اختبارات أو تجربة تفاعلية بعد إكمال هذه التعديلات. التنبيه الخارجي الفعلي عبر واتساب أو الاتصال التلقائي ما زال يحتاج ربط مزود اتصالات؛ النسخة الحالية تسجل قرار الإرسال داخل النظام وتعرضه لمشرف المبنى.

## الأمان وحسابات المستخدمين

- [x] إخفاء اختيار الدور وبيانات التجربة من صفحة الدخول؛ الدخول باسم المستخدم وكلمة المرور فقط، والدور يُقرأ من حساب المستخدم.
- [x] تسجيل الدخول عبر Firebase Authentication بدل التحقق داخل المتصفح.
- [x] حساب مدير أول `Admin` / `Admin123` يُنشأ عند أول دخول، ويُطلب تغيير كلمة المرور فورًا.
- [x] لوحة المدير: إضافة المستخدمين وتحديد أدوارهم، تغيير الدور، إيقاف/تفعيل الحساب، وإصدار كلمة مرور مؤقتة جديدة.
- [x] لوحة المدير: تعديل رقم السيارة واسم السائق وهاتفه ونوع السيارة، وإضافة وحذف السيارات.
- [x] قواعد Firestore حسب الدور (لا وصول لأي بيانات من دون حساب مفعّل).
- [x] تغيير كلمة المرور لكل مستخدم، وخروج تلقائي بعد ساعة من عدم النشاط وعند إغلاق المتصفح.
- [x] ترويسات أمان في Firebase Hosting، وإزالة الرابط العام `/manager`.

### خطوات مطلوبة في Firebase Console قبل النشر

1. Authentication ← Sign-in method: تفعيل **Email/Password**.
2. (مستحسن) تعطيل **Anonymous** لأنه لم يعد مستخدمًا.
3. النشر: `pnpm deploy` (ينشر الموقع وقواعد Firestore معًا).
4. بعد النشر مباشرة: الدخول بـ `Admin` / `Admin123` وتغيير كلمة المرور، ثم إنشاء حسابات الموظفين من لوحة المدير.

### تجربة محلية مع المحاكيات

`firebase emulators:start --only auth,firestore` ثم تشغيل الواجهة بـ `VITE_USE_FIREBASE_EMULATORS=true npx vite`.

## الخريطة وGPS والإحصائيات وتنظيم الطلبات

- [x] دليل المستشفيات (`shared/hospitals.ts`) مبني من وجهات ملف 11-07 إلى 23-08-2026، مع مطابقة الأسماء العربية والإنجليزية والأخطاء الإملائية (82% من الرحلات).
- [x] جمع الرحلات حسب القرب: نفس المستشفى +25، مستشفيات متجاورة ≤ 3 كم +20 (مثل مدينة حمد الطبية)، نفس الاتجاه ≤ 8 كم +10. لا تُجمع مواعيد أيام مختلفة.
- [x] تاريخ لكل موعد، ولا يمكن طلب سيارة بعد 30 دقيقة من الموعد حتى تعدّل العيادة الموعد.
- [x] استيراد ملف Excel لحركة السيارات من لوحة المدير: تُحفظ الإجماليات فقط (بلا أسماء أو أرقام مرضى)، وتحديث السيارات والسائقين من ورقة «قائمة».
- [x] لوحة السيارات: خريطة الدوحة (OpenStreetMap) بالمستشفيات والسيارات والرحلات الجارية، وإحصائيات سابقة.
- [x] دور «سائق» مرتبط بسيارة، يرسل موقع GPS من متصفح الهاتف.
- [x] المدير يصحح مواقع المستشفيات بالنقر على الخريطة ويضيف مستشفيات ومناطق وأسماء بديلة.

### ملاحظات

- إحداثيات المستشفيات الأولية تقريبية (دائرة متقطعة على الخريطة) ويجب تأكيدها من «دليل المستشفيات».
- GPS الهاتف يعمل ما دامت صفحة السائق مفتوحة والشاشة مضاءة. للتتبع الدائم يلزم جهاز تتبع في السيارة وربطه عبر خادم (Cloud Functions).
